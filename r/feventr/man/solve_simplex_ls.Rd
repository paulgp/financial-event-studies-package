% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver.R
\name{solve_simplex_ls}
\alias{solve_simplex_ls}
\title{Solve simplex-constrained least squares (synthetic-control weights)}
\usage{
solve_simplex_ls(
  A,
  b,
  V = NULL,
  method = c("hybrid", "fw", "qp"),
  support_size = NULL,
  eta = 0,
  max_iter = 2000,
  tol = 1e-12
)
}
\arguments{
\item{A}{Numeric matrix, t0 x n0 (columns = donors).}

\item{b}{Numeric vector, length t0.}

\item{V}{Optional time weighting: length-t0 vector (diagonal) or t0 x t0
positive semi-definite matrix; default identity.}

\item{method}{`"hybrid"` (Frank-Wolfe then exact QP restricted to the FW
support; default), `"fw"` (pure Frank-Wolfe), or `"qp"` (full OSQP —
quadratic in n0, for reference/small problems).}

\item{support_size}{Donors kept for the hybrid QP polish (top weights
after FW); default `min(n0, 5 * t0)`.}

\item{eta}{Ridge penalty on the weights.}

\item{max_iter, tol}{Frank-Wolfe iteration cap and minimum step size.}
}
\value{
`list(w, objective, iterations, support, method)`.
}
\description{
Minimizes `||A w - b||^2 + eta ||w||^2` over the unit simplex
(`w >= 0`, `sum(w) = 1`). `A` holds donor pre-event return paths in
columns; `b` is the treated-mean path.
}
