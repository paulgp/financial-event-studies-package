% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betas.R
\name{event_betas}
\alias{event_betas}
\title{Factor-loading comparison of treated and control units}
\usage{
event_betas(object, factors, time = NULL)
}
\arguments{
\item{object}{An `fes_fit` from [event_study()] with `keep_data = TRUE`.}

\item{factors}{Data frame: a time column (named as in the original panel,
or the first column) plus numeric factor columns. As in [event_study()],
any excess-return adjustment is the caller's responsibility.}

\item{time}{Name of the time column in `factors`; default its first column.}
}
\value{
A data.frame of class `fes_betas`: one row per group x statistic
  (mean, sd, median), one column per loading (alpha first).
}
\description{
Estimates per-unit OLS factor loadings over the fit's estimation window
and tabulates them by group: treated, control (equal-weighted), and —
when the fit carries synthetic-control unit weights — the omega-weighted
control portfolio (the paper's Tables 3, 4 and 7 diagnostics).
}
