% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_section.R
\name{cross_section}
\alias{cross_section}
\title{Cross-sectional event study with pre-event benchmarking}
\usage{
cross_section(
  data,
  unit,
  time,
  ret,
  event_time,
  chars = NULL,
  pre_window = c(-250, -1),
  method = c("ols", "gls"),
  npc = 100L,
  solver = c("cholesky", "woodbury"),
  min_return = 0.01,
  verbose = FALSE
)
}
\arguments{
\item{data}{Long panel: one row per unit-time.}

\item{unit, time, ret}{Column names (strings) in \code{data}. \code{ret} is the
short-horizon (e.g. one-day) event return that is regressed on \code{chars};
for \code{method = "gls"} it is also the series the covariance is built from,
so it should be a per-period (not cumulated) return.}

\item{event_time}{The value of \code{time} that is the event day. Time is
positional within the panel's sorted unique times (trading periods), as in
\code{\link[=event_study]{event_study()}}.}

\item{chars}{Character vector of characteristic column names (the regressors,
observed on each date; lag them yourself if desired). \code{NULL} fits an
intercept-only model (mean return per date).}

\item{pre_window}{Pre-event benchmark window in trading-period offsets,
\code{c(first, last)} with \verb{first < last < 0}, e.g. \code{c(-200, -1)}: the pseudo-
event days from \code{first} to \code{last} positions before the event.}

\item{method}{\code{"ols"} (default) or \code{"gls"}.}

\item{npc}{Principal components for the GLS covariance (default 100); must
not exceed the pre-window length.}

\item{solver}{GLS solver: \code{"cholesky"} (default, most precise) or
\code{"woodbury"} (faster for large cross-sections, slightly less precise).}

\item{min_return}{For GLS, firms whose absolute returns over the covariance
window sum to less than this are excluded (drops (near-)constant histories
that would make the covariance singular); default \code{0.01}.}

\item{verbose}{Print progress over the pre-event loop (the slow GLS path).}
}
\value{
An object of class \code{fes_cse}: \verb{$coefficients} (term, estimate,
\code{p_cdf}, \code{p_parametric}, pre-event mean/sd), \verb{$params} (named event-day
coefficients), \verb{$p_cdf}, \verb{$p_parametric}, \verb{$pre_betas} (L x k pseudo-event
coefficients), \verb{$n_obs} (firms per date, event first), \verb{$diagnostics},
\verb{$conventions}.
}
\description{
Estimates the event-day cross-sectional relationship between short-horizon
returns and firm characteristics, and benchmarks it against the distribution
of the same relationship on a window of pre-event ("pseudo-event") days.
This is the Cohn, Johnson, Liu & Wardlaw (2026) estimator, a different axis
from the treated-versus-counterfactual estimators in \code{\link[=event_study]{event_study()}}: the
unit of analysis is a single cross-sectional slope on the event day, not a
per-unit abnormal-return path.
}
\details{
On each date the model regresses \code{ret} on the characteristics \code{chars} (plus
an intercept) across all firms present that day:
\deqn{r_{i,t} = a_t + b_t' x_{i,t} + e_{i,t}.}
Confounding contemporaneous news makes the errors \code{e} cross-sectionally
correlated among firms with similar \code{x}, so the regression's own standard
error tests the wrong null (\eqn{b_t = 0}, "any relationship today") instead
of the null of interest ("the \emph{event} caused the relationship"), and
over-rejects on ordinary days. Instead of trusting that standard error, the
event-day slope \eqn{b_0} is compared to the empirical distribution of
\eqn{b_t} over the pre-event window: the counterfactual is the pre-event
slopes, not a fitted return path.

With \code{method = "gls"}, each date's regression is generalized least squares
using a return covariance estimated from the leading principal components of
that date's own preceding-return window (\eqn{\Omega = V\,\mathrm{diag}
(\lambda)\,V' + \mathrm{diag}(d)}), which down-weights common-factor
comovement and sharply increases power over OLS. The GLS covariance window
\emph{rolls} with each pseudo-event date, so the panel must extend
\code{-pre_window[1]} further back than \code{pre_window[1]} itself.
}
\references{
Cohn, J. B., Johnson, T. L., Liu, Z. & Wardlaw, M. I. (2026).
Past is Prologue: Inference from the Cross Section of Returns Around an
Event. \emph{Journal of Financial Economics}.
}
