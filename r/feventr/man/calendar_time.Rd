% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calendar_time.R
\name{calendar_time}
\alias{calendar_time}
\title{Calendar-time portfolio event study}
\usage{
calendar_time(
  data,
  unit,
  time,
  ret,
  events,
  window = c(0, 10),
  factors = NULL,
  returns,
  align = c("position", "value"),
  weight = NULL,
  min_units = 1L,
  se = c("ols", "nw"),
  lag = NULL
)
}
\arguments{
\item{data, unit, time, ret}{As in \code{\link[=event_study]{event_study()}}: one long panel covering
every event's window.}

\item{events}{Data frame with columns \code{unit} and \code{event_time}, one row per
event, as in \code{\link[=event_study_batch]{event_study_batch()}}. A unit appearing in several
overlapping windows enters the portfolio once per period.}

\item{window}{Portfolio membership window in event-time offsets, e.g.
\code{c(0, 10)}: a unit is held from \code{window[1]} to \code{window[2]} trading
periods after its event.}

\item{factors}{Data frame with the panel \code{time} column plus numeric factor
columns covering every portfolio period (as in \code{\link[=event_study]{event_study()}}; the
excess-return convention is the caller's responsibility). \code{NULL} fits an
intercept-only model: alpha is the mean portfolio return, for use when
\code{ret} is already an abnormal or excess return.}

\item{returns}{\code{"simple"} or \code{"log"}; required, no default.}

\item{align}{\code{"position"} (default; event/calendar times count positions
among the panel's sorted unique times) or \code{"value"} (numeric time values
used directly), as in \code{\link[=event_study]{event_study()}}.}

\item{weight}{Optional column name in \code{data} holding portfolio weights
(e.g. lagged market cap) for a value-weighted portfolio; default
equal-weighted.}

\item{min_units}{Calendar periods whose portfolio holds fewer units are
dropped from the regression (Fama (1998) uses 10).}

\item{se}{\code{"ols"} (classical) or \code{"nw"} (Newey-West with Bartlett kernel).}

\item{lag}{Newey-West lag; default \code{floor(4 * (n/100)^(2/9))}.}
}
\value{
An object of class \code{fes_caltime}: \verb{$alpha}/\verb{$alpha_se} (per-period
abnormal return), \verb{$coefficients} (regression table), \verb{$portfolio}
(calendar-period series: return, units held, abnormal return), \verb{$nobs}.
}
\description{
For every calendar period, forms a portfolio of all units whose event
occurred within \code{window} event-time offsets, and regresses the portfolio
return on the supplied factors. The intercept is the average abnormal
return per calendar period — the Jaffe-Mandelker calendar-time portfolio
estimator advocated by Fama (1998) for event studies with many staggered
events, where overlapping event windows make event-time CARs
cross-sectionally dependent; aggregating to one portfolio return per
calendar period absorbs that dependence into the time series.
}
