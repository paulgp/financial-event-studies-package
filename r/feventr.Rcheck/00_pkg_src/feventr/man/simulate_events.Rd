% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_events}
\alias{simulate_events}
\title{Simulate an event-study return panel with a two-factor structure}
\usage{
simulate_events(
  n_units = 500,
  n_pre = 239,
  n_candidate = 250,
  n_post = 10,
  tau = 0.03,
  treat_share = 0.1,
  beta_mean = c(mkt = 1, smb = 1),
  beta_sd = c(mkt = 0.3, smb = 0.3),
  resid_sd = NULL,
  selection = c("none", "assignment", "timing", "both"),
  factors = NULL,
  seed = NULL
)
}
\arguments{
\item{n_units}{Number of firms.}

\item{n_pre}{Estimation periods before the earliest candidate event day.}

\item{n_candidate}{Candidate event days (event day 0 is the first when
timing is random).}

\item{n_post}{Post-event periods simulated after the candidate window.}

\item{tau}{Treatment effect added on the event day.}

\item{treat_share}{Average treated share.}

\item{beta_mean, beta_sd}{Length-2 (mkt, smb) loading distribution params.}

\item{resid_sd}{Idiosyncratic sd; \code{NULL} = paper's 50\%-variance rule.}

\item{selection}{One of \code{"none"}, \code{"assignment"}, \code{"timing"}, \code{"both"}.}

\item{factors}{Data frame with columns \code{mktrf}, \code{smb}, \code{rf} (decimal daily
returns); default the bundled \code{ff_daily} (Ken French daily factors,
1926-07-01..2022-11-30, the paper's sample).}

\item{seed}{Optional RNG seed.}
}
\value{
A list of class \code{fes_sim}: \code{data} (long panel: id, t, ret),
\code{events} (treated unit ids + event_time, ready for \code{event_study()}),
\code{betas} (per-unit loadings and treatment flag), \code{factors} (per-period
realized mktrf, smb, rf), \code{event_time}, \code{tau}.
}
\description{
Returns r_it = beta_mkt_i * MktRF_t + beta_smb_i * SMB_t + rf_t + eps_it,
with a one-shot treatment effect \code{tau} added on the event day for treated
units. Factor days are sampled jointly (whole rows, preserving the
contemporaneous MktRF-SMB correlation) without replacement from \code{factors}.
The residual sd defaults to the paper's rule that idiosyncratic variance
equals systematic variance: sqrt(var(MktRF) + var(SMB) + 2 cov).
}
\details{
Selection mechanisms (paper Section 4.1): \code{"assignment"} treats units by a
logit in beta_smb scaled so that on average \code{treat_share} are treated
(lower loadings more likely treated); \code{"timing"} places the event on the
day with the largest SMB realization among the \code{n_candidate} candidate
days (the paper's logit-timing draw is vestigial dead code in the
published DGP, but it is replayed here because it consumes RNG).
}
